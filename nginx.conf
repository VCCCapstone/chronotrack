server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Flutter web routing
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Serve static assets directly
  location /assets/ {
    try_files $uri =404;
  }
}
